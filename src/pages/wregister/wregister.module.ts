import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { WregisterPage } from './wregister';

@NgModule({
  declarations: [
    WregisterPage,
  ],
  imports: [
    IonicPageModule.forChild(WregisterPage),
  ],
})
export class WregisterPageModule {}
